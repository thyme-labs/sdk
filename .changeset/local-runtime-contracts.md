---
'@thyme-labs/sdk': minor
---

Expose lifecycle callback contracts and an isolate-safe task archive reader so
the Thyme backend and console can consume the same runtime definitions as the
public SDK.
