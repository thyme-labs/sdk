---
'@thyme-labs/sdk': minor
---

Expose lifecycle callback contracts, source-schema extraction, and an
isolate-safe task archive reader so the Thyme backend, CLI, and console consume
the same runtime definitions as the public SDK.
